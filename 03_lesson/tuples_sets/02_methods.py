L_test = [1, 2, 3, 4]
T_test = (1, 2, 3, 4)
S_test = {1, 2, 3, 4}

#Działają dla tuple:
T_test.index(1) # 1 jako element
T_test.count(1) # 1 jako element
reversed(T_test) #brzmi nieco inaczej dla list: reverse()

#Dla setów nie działają.